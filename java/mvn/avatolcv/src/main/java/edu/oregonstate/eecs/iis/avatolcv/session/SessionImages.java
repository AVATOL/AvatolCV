package edu.oregonstate.eecs.iis.avatolcv.session;

import java.util.ArrayList;

/**
 * Originally, SessionImages was intended to be thinned out as images were excluded for various reasons.
 * I later decided that since we will want to know about the excluded images later  (i.e. show them, 
 * but greyed-out, for example) that we will just keep them all in play for the duration.
 */
public class SessionImages extends ArrayList<String> {

}
